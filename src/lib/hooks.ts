import { Dispatch, SetStateAction, useCallback } from 'react';

export const useWithLocalStorage = <T>(setStateFn: Dispatch<SetStateAction<T>>) => {
  // return useCallback(() => {
  //   setStateFn
  // });
}
